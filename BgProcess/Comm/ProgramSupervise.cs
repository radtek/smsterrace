using System;
using System.Collections.Generic;
using System.Text;
using System.Threading;

namespace hz.sms.Comm
{
    class ProgramSupervise
    {
        public void get()
        {
            /*
            //--------------------
            System.Diagnostics.Process[] pApp = System.Diagnostics.Process.GetProcessesByName("西安电信");//GetProcesses();

            if (pApp.Length > 1)
            {
                return;
            } 

            //----------------------
            
            string[] p = Environment.GetCommandLineArgs();
            if (p.Length < 2)
            {
                bool bCreatedNew;
                Mutex m = new Mutex(true, "keywordcompetitionsel###", out bCreatedNew);
                if (bCreatedNew)
                {
                    Form1 f = new Form1();
                    Application.Run(f);
                }
            }
            else
            {
                Application.Run(new Form1());
            }

            //--------------------
             */
        }
    }
}
